<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ang.latihan.json"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="15" />

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:configChanges="keyboardHidden|orientation"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".Splash"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Semua Anggota Activity -->
        <activity
            android:name=".DetailCandiActivity"
            android:label="Semua Anggota" />
        <activity
            android:name=".HalamanUtamaActivity"
            android:label="Halaman Utama " />
        <activity
            android:name=".DaftarCandiActivity"
            android:label="Semua Candi" />
        <!-- Tambah Anggota Activity -->
        <activity
            android:name=".TambahEventActivity"
            android:label="Tambah Anggota" />

        <!-- Edit Anggota Activity -->
        <activity
            android:name=".EditEventActivity"
            android:label="Edit Anggota" />
        <activity
            android:name=".DaftarEventActivity"
            android:label="@string/title_activity_daftar_event" />
    </application>

</manifest>
